# CSU 课表 (CSU Schedule)

🚀 一款专为中南大学 (CSU) 学生打造的轻量、纯净、高颜值的 Flutter 课表应用。

## ✨ 核心特性

* **一键导入**：内置 WebView 授权登录，直连中南大学教务系统，自动抓取并解析课表。
* **智能排课**：自动识别单双周、连续周次，智能合并同名断层课程。
* **交互式编辑**：支持长按拖拽调整课程时间，下拉空白网格快速添加自定义课程，底部丝滑拖拽删除。
* **高度个性化**：内置 HSV 专业色轮，支持全局背景色自定义（支持 HEX 代码）。独创的动态悬浮算法，边框线条会根据你的背景色自动计算出完美融合的深色系。
* **离线缓存**：基于 `shared_preferences` 的本地持久化存储，无网状态下依然秒开。

## 🛠️ 技术栈

* **框架**: Flutter (Dart)
* **网络与解析**: `http`, `html` (替代 Python BeautifulSoup 的纯 Dart 解析方案)
* **本地存储**: `shared_preferences`
* **UI 增强**: `flutter_colorpicker`, `webview_flutter`

## 📖 使用方法

1. **一键导入课表**：
    * 点击主界面右上角的“下载”图标，在弹出的教务系统网页完成登录。
    * 登录成功后，系统会自动抓取并生成课表。
    * 随后会弹出日历，请选择本学期开学第一周的任意一天，系统会自动推算周一并对齐所有周次。

2. **个性化主题色**：
    * 点击右上角的“调色板”图标，唤出专业 HSV 色轮。
    * 拖拽选色或直接输入 HEX 代码，挑选你最心仪的背景色。

3. **灵活调整与编辑**：
    * **移动课程**：长按已有课程卡片，将其拖拽至目标空白时间段。
    * **添加课程**：在空白网格处长按并向下滑动，动态红色方块会提示你框选的节次长度，松手即可进入添加页面。
    * **删除课程**：长按课程卡片，屏幕底部会浮现红色垃圾桶，拖入其中即可删除。
    * **查看与复制**：单击课程卡片查看详细信息；在详情页内长按任意文本（如教师、教室）即可一键复制。

## 📦 编译与运行

1. 确保已安装 Flutter 环境。
2. 克隆本项目：
   ```bash
   git clone [https://github.com/CowHorseQiao/csu-schedule.git](https://github.com/CowHorseQiao/csu-schedule.git)
3. 获取依赖并运行：
    ```bash
   flutter pub get
   flutter run 

本项目遵循 MIT License 开源协议。